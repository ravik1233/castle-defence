Drop painted PNGs here, named after the texture key they replace
(e.g. `unit.orc.full.png`), then run:

    npm run art:import

See ../docs/GEMINI_ART_PROMPTS.md for the prompts and the full key list.
